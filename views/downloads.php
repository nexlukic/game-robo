<?phpinclude('../konekcija.php');if(isset($_REQUEST['id'])){    $id = $_REQUEST['id'];    $upit = "UPDATE igrice SET broj = broj + 1 WHERE id = :id;";    $updated = $kon->prepare($upit);    $updated->bindParam(":id",$id);    $updated->execute();    if($updated) {        $upit = "SELECT * FROM igrice;";        $result = $kon->query($upit);        if ($result) {            $upit_ukupno = "SELECT broj FROM igrice where id = $id;";            $rezultat = $kon->query($upit_ukupno);            $r=$rezultat->fetchObject();             echo $r->broj.' '.'Downloads';        }    }}