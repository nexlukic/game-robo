<?phpif(isset($_REQUEST['dodajslajder'])){    $putanja="images/".time().basename( $_FILES['file']['name']);    $alt=$_REQUEST['alt'];    $target = "../images/";    $target = $target.time() . basename( $_FILES['file']['name']);    $file1=($_FILES['file']['name']);    if(move_uploaded_file($_FILES['file']['tmp_name'], $target))  {//Tells you if its all ok        $upit="INSERT INTO slajder(putanja,alt) VALUES(:putanja,:alt)";        $stmt=$kon->prepare($upit);        $stmt->bindParam(":putanja",$putanja);        $stmt->bindParam(":alt",$alt);        try{            $stmt->execute();        }catch (PDOException $ex){            echo $ex->getMessage();        }    }    else {//Gives and error if its not  echo "Sorry, there was a problem uploading your file.";      }    }}?><form class="form-horizontal" action="admin.php?page=dodajslajder" method="POST" enctype="multipart/form-data">    <fieldset>        <div id="legend">            <legend class="">Dodavanje Slajdera</legend>        </div>        <div class="control-group">            <!-- E-mail -->            <label class="control-label" >Dodaj sliku</label>            <div class="controls">                <input type="file" name="file">            </div>        </div>        <div class="control-group">            <!-- Username -->            <label class="control-label"  for="username">Alt</label>            <div class="controls">                <input type="text" id="username" name="alt" placeholder="" class="input-xlarge">            </div>        </div        <div class="control-group">            <!-- Button -->            <div class="controls">                </br>                <button type="submit" class="btn btn-success" name="dodajslajder">Dodaj</button>            </div>        </div>    </fieldset></form>